<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_bg"
    tools:context=".ScheduleActivity">
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/schedule_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/Theme.ToDo"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:menu="@menu/main_menu"
        app:title="on.time"
        app:titleTextColor="@color/white">

        <ImageView
            android:id="@+id/schedule_toolbar_remindersIcon"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_gravity="end"
            android:layout_marginEnd="10dp"
            android:contentDescription="@string/remindersIcon"
            android:src="@drawable/bell"
            app:layout_constraintRight_toRightOf="@id/toolbar"
            />
    </androidx.appcompat.widget.Toolbar>

    <CalendarView
        android:id="@+id/scheduleCalendar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_margin="10dp"
        app:layout_constraintTop_toBottomOf="@id/schedule_toolbar"
        />

    <com.leinardi.android.speeddial.SpeedDialView
        android:id="@+id/scheduleExpandedFAB"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:backgroundTint="@color/noteBackground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:sdMainFabClosedSrc="@drawable/add"
        app:sdMainFabClosedBackgroundColor="@color/noteBackground"
        app:sdMainFabOpenedBackgroundColor="@color/noteBackground"/>
</androidx.constraintlayout.widget.ConstraintLayout>